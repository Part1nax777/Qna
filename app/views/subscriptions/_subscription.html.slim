- if current_user&.has_subscribe?(resource)
  = link_to 'Unsubscribe', subscription_path(current_user.subscriptions.find_by(question: resource)), method: :delete, remote: true
- else
  = link_to 'Subscribe', question_subscriptions_path(resource), method: :post, remote: true