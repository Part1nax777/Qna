- if current_user&.has_subscribe?(resource)
  = link_to 'Unsubscribe', subscription_path(current_user.get_subscribe(resource)), method: :delete, remote: true
- else
  = link_to 'Subscribe', question_subscriptions_path(resource), method: :post, remote: true