$('.question-errors').html('<%= render 'shared/errors', resource: @question %>');

<% if !@question.errors.present? %>
$('.question-show').html('<%= j render @question %>');
$('#edit-question').addClass('hidden');
$('.edit-question-link').show();
<% end %>